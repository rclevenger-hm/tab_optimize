{
  "name": "tab-optimize",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that suspends inactive background tabs using Chrome's native discard API.",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
